import React from 'react';
import LoginPage from './LoginPage';

const Home: React.FC = () => {
  return (
    <div>
      {/* Other content for your homepage (optional) */}
      <LoginPage />
    </div>
  );
};

export default Home;